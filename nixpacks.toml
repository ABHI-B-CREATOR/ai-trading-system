[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "cd 8_BACKEND_APPLICATION_LAYER && python3.11 -m gunicorn app_server:app --bind 0.0.0.0:${PORT:-5000} --workers 1 --threads 1 --timeout 120"
